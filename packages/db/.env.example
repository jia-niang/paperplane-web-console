# 此文件仅为示例，内容不会被读取，请勿修改此文件

# 建议：
#   仅生产环境的配置项，使用 .env.production.local  文件
#   仅开发环境的配置项，使用 .env.development.local 文件
# （这些文件名以 .local 结尾，它们不会被 Git 提交）

# PostgreSQL 数据库连接字符串
#   示例： postgresql://postgres:password@localhost:5432/db_paperplane?schema=public
POSTGRESQL_URL =
